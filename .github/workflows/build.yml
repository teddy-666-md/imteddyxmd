name: Build and Publish

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo A
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Restore obfuscator cache
        uses: actions/cache@v4
        with:
          path: .obfuscate-cache.json
          key: obfuscate-cache-${{ github.sha }}
          restore-keys: |
            obfuscate-cache-

      - name: Install dependencies
        run: npm install

      - name: Obfuscate source
        run: npm run build

      - name: Checkout Repo B
        uses: actions/checkout@v4
        with:
          repository: supreme-Lord2/xjx
          token: ${{ secrets.REPO_B_TOKEN }}
          path: repo-b

      - name: Replace Repo B with obfuscated build
        run: |
          find repo-b -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          cp -a "teddy-xmd-build/." repo-b/

      - name: Push to Repo B
        run: |
          cd repo-b

          git config user.name "TEDDY-XMD Bot"
          git config user.email "junexbot@users.noreply.github.com"

          git add .

          if git diff --cached --quiet; then
            echo "No changes to publish."
          else
            git commit -m "Update obfuscated build"
            git push
          fi
